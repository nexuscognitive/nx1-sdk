# This workflow installs the latest version of Terraform CLI and configures the Terraform CLI configuration file
# with an API token for Terraform Cloud (app.terraform.io). On pull request events, this workflow will run
# `terraform init`, `terraform fmt`, and `terraform plan` (speculative plan via Terraform Cloud). On push events
# to the "main" branch, `terraform apply` will be executed.
#
# Documentation for `hashicorp/setup-terraform` is located here: https://github.com/hashicorp/setup-terraform
#
# To use this workflow, you will need to complete the following setup steps.
#
# 1. Create a `main.tf` file in the root of this repository with the `remote` backend and one or more resources defined.
#   Example `main.tf`:
#     # The configuration for the `remote` backend.
#     terraform {
#       backend "remote" {
#         # The name of your Terraform Cloud organization.
#         organization = "example-organization"
#
#         # The name of the Terraform Cloud workspace to store Terraform state files in.
#         workspaces {
#           name = "example-workspace"
#         }
#       }
#     }
#
#     # An example resource that does nothing.
#     resource "null_resource" "example" {
#       triggers = {
#         value = "A example resource that does nothing!"
#       }
#     }
#
#
# 2. Generate a Terraform Cloud user API token and store it as a GitHub secret (e.g. TF_API_TOKEN) on this repository.
#   Documentation:
#     - https://www.terraform.io/docs/cloud/users-teams-organizations/api-tokens.html
#     - https://help.github.com/en/actions/configuring-and-managing-workflows/creating-and-storing-encrypted-secrets
#
# 3. Reference the GitHub secret in step using the `hashicorp/setup-terraform` GitHub Action.
#   Example:
#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v1
#       with:
#         cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

name: NX1 App Deploy

on:
  push:
    branches: [dd_test_git_pipeline]
  workflow_dispatch:
    inputs:
      app_name:
        description: 'App name'
        required: true
      version:
        description: 'App version'
        required: true
      nx1_api_key:
        description: 'API KEY'
        required: true
      nx1_host:
        description: 'NX1 HOST URL'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install NX1 SDK
        run: pip install git+https://github.com/nexuscognitive/nx1-sdk

      - name: Deploy App
        env:
          NX1_API_KEY: ${{ github.event.inputs.nx1_api_key }}
          NX1_HOST: ${{ github.event.inputs.nx1_host }}
        run: |
          nx1 apps deploy \
            --app-name "${{ github.event.inputs.app_name || 'my-data-app' }}" \
            --version "${{ github.event.inputs.version || github.sha }}" \
            --dags https://github.com/DarshiDoshi/dags_and_artifacts/blob/main/dags/dag1_dd.py \
            --artifacts https://github.com/DarshiDoshi/dags_and_artifacts/blob/main/artifacts/artifact2.py
